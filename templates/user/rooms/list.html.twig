{% extends "base.html.twig" %}

{% block body %}
<div class="container">
    <h1>Mes salles</h1>

    <table class="table">
        {% for room in rooms %}
        <tr>
            <td>
                <a class=" text-secondary text-decoration-none" href="{{ path('app_user_room_details', {'id': room.id}) }}">{{ room.name }}</a>
            </td>
        </tr>

        {% endfor %}
    </table>
    <a href="{{ path('app_user_create_room') }}" class="btn btn-success">Créer une nouvelle salle</a>
</div>
{% endblock %}
